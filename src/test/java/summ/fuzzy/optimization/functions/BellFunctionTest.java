package summ.fuzzy.optimization.functions;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

public class BellFunctionTest {

	@Test
	void test() {
		
//		BellFunction bf = new BellFunction();
//		
//		double vec[] = {1.0, 3.0, 4.0};
//		boolean res = bf.isFeasibleValue(0.5, 2.0, vec); // 1.5 > 1 && 2.5 < 3.0
//		assertEquals(true, res); 
//		res = bf.isFeasibleValue(1.0, 2.0, vec);
//		assertEquals(false, res);
//		res = bf.isFeasibleValue(0.5, 0.0, vec); // menor que o primeiro elemento
//		assertEquals(true, res);
//		res = bf.isFeasibleValue(0.5, 5.0, vec); // maior que o último
//		assertEquals(true, res);
//		res = bf.isFeasibleValue(0.2, 3.0, vec); 
//		assertEquals(false, res);
//		res = bf.isFeasibleValue(0.5, 3.5, vec); 
//		assertEquals(false, res);
		
	}
	
}
